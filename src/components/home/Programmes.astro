---
interface Card {
  title: string;
  age: string;
  blurb: string;
  href: string;
}
interface Props {
  cards: Card[];
}
const { cards } = Astro.props;
---

<section class="py-10 lg:py-16 border-t border-black/10">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-2xl sm:text-3xl font-semibold tracking-tight">
      Programmes
    </h2>
    <p class="mt-2 text-black/70 max-w-prose">
      We run two programmes that move from first contact to long-term practice.
    </p>
    <div class="mt-8 grid gap-6 md:grid-cols-2">
      {
        cards.map((c) => (
          <a
            href={c.href}
            class="group block card p-6 transition-all hover:shadow-[0_8px_0_#000] hover:-translate-y-1"
          >
            <div class="text-xs uppercase tracking-wide text-[--color-brand]">
              {c.age}
            </div>
            <div class="h3 mt-1">{c.title}</div>
            <p class="mt-2 text-black/75">{c.blurb}</p>
            <div class="mt-4 inline-flex items-center gap-2 text-sm font-medium group-hover:underline decoration-[--color-brand] underline-offset-4">
              Explore {c.title} <span aria-hidden>→</span>
            </div>
          </a>
        ))
      }
    </div>
  </div>
</section>
